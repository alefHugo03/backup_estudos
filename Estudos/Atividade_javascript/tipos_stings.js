estudante = "Caroline";
console.log(`A estudante se chama ${estudante}`);
// Usando crase `` para inserir variáveis dentro de strings

const senha = "senhaSegura123" + estudante.toUpperCase();
console.log(senha);
// toUpperCase() deixa todas as letras maiúsculas
// toLowerCase() deixa todas as letras minúsculas

//typeof() mostra o tipo da variável